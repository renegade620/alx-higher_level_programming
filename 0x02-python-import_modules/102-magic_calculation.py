#!/usr/bin/env python3
from magic_calculation_102 import add, sub


def magic_calculation(a, b):
    if (x < y):
        z = add(x, y)

        for i in range(4, 6):
            z = add(z, i)

        return z
    else:
        return sub(x, y)
